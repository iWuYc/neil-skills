# AGENTS.md

This file is a single-line pointer. All repository guidance (purpose, architecture, conventions, common commands) lives in [`CLAUDE.md`](CLAUDE.md). Do not duplicate content here — Codex and Claude Code share the same authoritative doc.

## Why this file exists

Codex looks for `AGENTS.md` in the repo root; Claude Code looks for `CLAUDE.md`. Keeping two near-identical guides in sync is a known drift hazard, so this file intentionally has no body content beyond the pointer. Update `CLAUDE.md` and the change is visible to both tools.
