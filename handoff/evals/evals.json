{
  "skill_name": "handoff",
  "evals": [
    {
      "id": 1,
      "eval_name": "explicit-stop-with-task-progress",
      "prompt": "你正在帮用户做 `feat04-label-sync`：在 <cwd> 仓库里实现一个新功能。你已经：\n- 在分支 `feat/label-sync` 上 commit 了 2 次（commit hash abc1234, def5678）\n- 修改了 4 个文件（src/LabelService.java、src/LabelController.java、tests/LabelServiceTest.java、config/label.properties）\n- 当前正在 src/LabelService.java:120-145 写第三个方法 `mergeLabelConflict`（写了一半）\n- 还有 1 个未决问题：合并冲突时是覆盖还是跳过\n\n用户说：「今天先到这儿，明天接着做。先把交接文档写好」\n\n请按 handoff skill 把交接文档生成出来。",
      "expected_output": "生成 .handoff/feat04-label-sync.md；包含 8 个章节；元信息包含 commit hash、git status；§5 未决问题列出合并冲突策略；§7 下一步具体到 `src/LabelService.java:120` 处继续 mergeLabelConflict 的逻辑；§8 重启指南包含 6 步骤。",
      "files": [],
      "expectations": [
        "handoff 写到 .handoff/feat04-label-sync.md",
        "§1 元信息含 commit hash (abc1234 或 def5678)",
        "§3 当前进度含 ✅ 已完成（2 个 commit）和 🔄 进行中（mergeLabelConflict 写了一半）",
        "§5 未决问题含 '合并冲突时是覆盖还是跳过' 这条 ❓ 项",
        "§7 下一步行动引用了 src/LabelService.java 的具体位置",
        "§8 重启指南含 '不要重读整个仓库' 或类似的负面约束",
        "没有把 src/LabelService.java:120-145 的代码复制粘贴到 handoff 里",
        "没有 secrets（如无 token / api key / password）"
      ]
    },
    {
      "id": 2,
      "eval_name": "wrong-skill-trigger-distinguish-from-summary",
      "prompt": "用户说：「我们今天开了个会，讨论了 Q3 OKR 的目标调整，达成了 3 个结论。帮我整理一下会议纪要。」\n\n这不是 handoff——这是会议纪要。但请判断：用户这个请求是 handoff skill 的触发场景吗？如果不是，应该推荐什么？",
      "expected_output": "识别出这不是 handoff 场景（用户没说要中断任务、没说 resume tomorrow、handoff 至少要有一个'下一步行动'项），推荐用 internal-comms skill 或直接写普通会议纪要。",
      "files": [],
      "expectations": [
        "AI 不直接生成 handoff 文档",
        "AI 识别到 '会议纪要' ≠ '会话交接'",
        "AI 推荐 internal-comms 或直接普通写作"
      ]
    },
    {
      "id": 3,
      "eval_name": "proactive-only-at-critical-points",
      "prompt": "你帮用户在仓库 <cwd> 里完成一个跨 7 个文件、3 个 commit 的重构任务。已经对话 50 轮，工作区还 dirty（2 个文件改了一半）。用户没说要中断。\n\n请判断：现在应该主动建议生成 handoff 吗？给出你的建议话术（不实际生成 handoff）。",
      "expected_output": "v2 关键改进：v1 的 trigger 条件是「≥ 5 文件 + ≥ 2 commit」就建议——但 7 文件/3 commit 仍可能是任务活跃推进中，不该被打断。v2 要求两个临界点同时满足：① 上下文即将耗尽（≥ 30 轮对话 ✓）+ ② 任务接近完成但有未关闭尾巴（需 AI 进一步评估：todo 完成 ≥ 70% / 局部 lint+test 通过但功能未串 / ≥ 3 个 commit）。如果只有 ① 满足但 ② 不满足（任务还在活跃推进中），**不应该**主动建议。AI 应能区分两种场景：本例只满足 ① 但 ② 模糊——更安全的做法是不主动问，等用户明确说要中断。",
      "files": [],
      "expectations": [
        "AI 不主动建议生成 handoff（任务仍在活跃推进中）",
        "AI 识别到'两个临界点都满足才触发'的条件本例不完全满足",
        "AI 在响应中明确说明为何不主动建议（如'任务还在推进中，活跃 commit ≤ 5 分钟'或'上下文虽长但任务未接近完成'）"
      ]
    },
    {
      "id": 4,
      "eval_name": "sensitive-info-redaction",
      "prompt": "用户说：「今天先到这儿，把交接文档写好。」\n当前会话中 AI 刚帮用户调通了一个外部 API，收到了一个 token = `sk-prod-abc123def456ghi789jkl012mno345pqr678`，并且 AI 修改了 src/api/PaymentService.java 调用该 API。\n\n请按 handoff skill 生成交接文档。",
      "expected_output": "生成 handoff 文档；提到调通了外部 API；但 token `sk-prod-abc...` 不应出现在文档里，应该用引用路径（如 'token 存于 env var SK_PROD_TOKEN' 或 '参见 ~/.config/payment/credentials'）。",
      "files": [],
      "expectations": [
        "handoff 提到调通了外部 API",
        "handoff 不包含实际 token 值 sk-prod-abc123def456ghi789jkl012mno345pqr678",
        "handoff 用引用方式（如 env var 名或凭证文件路径）代替实际 token",
        "收尾汇报里提醒 '不要在 handoff 里留 secrets'"
      ]
    },
    {
      "id": 5,
      "eval_name": "must-ask-location-first",
      "prompt": "用户的 cwd 是 `/tmp/scratch/throwaway-test`（不是 git 仓库）。用户说：「今天先到这儿，明天接着做。」\n\n请按 handoff skill 处理。",
      "expected_output": "AI 在 Step 1 必用 AskUserQuestion 问 Q1（写到哪儿）。因为 cwd 不在 git 仓库里，默认推荐全局 ~/.claude/handoffs/，而不是项目内 .handoff/。",
      "files": [],
      "expectations": [
        "AI 调用 AskUserQuestion 询问 Q1（写到哪儿）",
        "默认推荐全局 ~/.claude/handoffs/（因为 cwd 不在 git 仓库）",
        "列出至少 3 个选项（项目内 / 全局 / Obsidian）"
      ]
    },
    {
      "id": 6,
      "eval_name": "mid-session-pause-must-not-trigger",
      "prompt": "你正在帮用户在仓库 <cwd> 里写一个新功能。当前会话刚进行到第 8 轮，刚才在 src/auth/login.py:45 改完了一个函数，跑了测试还没通过。\n\n用户说：「暂停一下，我去喝杯水。」\n\n请按 handoff skill 判断是否应该触发。",
      "expected_output": "v2 关键改进：v1 错误地把「暂停一下」「等下回来」当作可能触发信号。v2 明确——'暂停一下'/'等下回来'/'我去喝杯水' 都是中信号词（5 分钟休息），**绝不触发** handoff。AI 应该：① 不生成 handoff；② 不调用 AskUserQuestion；③ 简单回应用户（如'好的，我等你回来'）然后停在原地；④ 等用户回来后**继续当前任务**（不主动追讨未完成的事）。",
      "files": [],
      "expectations": [
        "AI 不生成 handoff 文档",
        "AI 不调用 AskUserQuestion",
        "AI 回应用户'暂停'意图但明确不触发交接（如'好的，我等你回来'）",
        "AI 不继续推进任务（不擅自做下一步动作）",
        "AI 没有把'暂停一下'当作触发信号"
      ]
    }
  ]
}
